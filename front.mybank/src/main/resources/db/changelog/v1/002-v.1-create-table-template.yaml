databaseChangeLog:
  - preConditions:
      - onFail: HALT
      - onError: HALT
  - changeSet:
      id: "Init_DB_Front_Table"
      author: Vladimir P.
      description: Инициализация БД.
      labels: Initial DB
      context: Front_MyBank_v.1
      comment: Создание таблицы из шаблона.
      tagDatabase:
        tag: create_table_template
      changes:
        - preConditions:
            - not:
                - tableExists:
                    schemaName: frontscheme
                    tableName: template
        - createTable:
            schemaName: frontscheme
            tableName: template
            columns:
              - column:
                  name: template_id
                  type: bigserial
                  autoIncrement: true
                  constraints:
                    primaryKey: true
                    nullable: false
      rollback:
        - dropTable:
            schemaName: frontscheme
            tableName: template
