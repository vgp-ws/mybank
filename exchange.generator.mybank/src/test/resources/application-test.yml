server:
    port: 8081
    servlet:
        context-path: /template.mybank
spring:
    application:
        name: template

    cloud:
        consul:
            enabled: false
            config:
                enabled: false
            discovery:
                enabled: false

    # security:
    #   oauth2:
    #     client:
    #       provider:
    #         keycloak:
    #           issuer-uri: http://mock-keycloak:8080/realms/master
    #       registration:
    #         mybank:
    #           provider: keycloak
    #           client-id: test-client
    #           client-secret: test-secret
